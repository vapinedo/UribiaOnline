import { createFeatureConfig } from '@core/factories/createFeatureConfig';

export const formatoRPPConfig = createFeatureConfig({
  name: 'formatoRPP',
  collectionName: 'formatoRPPs',
  routePath: '/formato-rpp',
});
