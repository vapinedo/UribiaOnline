import { createFeatureConfig } from '@core/factories/createFeatureConfig';

export const propiedadConfig = createFeatureConfig({
  name: 'propiedad',
  collectionName: 'propiedades',
  routePath: '/propiedades',
});