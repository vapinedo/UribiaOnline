import { createFeatureConfig } from '@core/factories/createFeatureConfig';

export const fundacionConfig = createFeatureConfig({
  name: 'fundacion',
  collectionName: 'fundaciones',
  routePath: '/fundaciones',
});
