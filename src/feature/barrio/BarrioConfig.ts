import { createFeatureConfig } from '@core/factories/createFeatureConfig';

export const barrioConfig = createFeatureConfig({
  name: 'barrio',
  collectionName: 'barrios',
  routePath: '/barrios',
});
